/* Author: Casey Lafferty
 * SharedHeaders.h allows for a generic list of headers to be
 *   listed in one file to clean up other header files
 */

#pragma once

// used for standard input/output (cin/cout)
#include <iostream>

// used for file IO
#include <fstream>

// used to reference the current computer time
#include <time.h>

// used to reference the standard IO handle for changing output color
#include <windows.h>